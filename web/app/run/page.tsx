import { RunScreen } from "@/components/RunScreen";

export const dynamic = "force-dynamic";

export default function RunPage() {
  return (
    <main>
      <div className="kicker">Run</div>
      <RunScreen />
    </main>
  );
}
