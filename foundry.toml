[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
# Transient storage (Cancun) is not relied on; shanghai keeps the surface small.
evm_version = "shanghai"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
arc_testnet = "${ARC_TESTNET_RPC_URL}"

[fmt]
line_length = 110
